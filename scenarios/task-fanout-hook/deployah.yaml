# $schema: ../../internal/spec/schema/v1-alpha.5/manifest.json
apiVersion: v1-alpha.5
project: task-fanout-hook
components:
  api:
    image: nginx:latest
    port: 8080
    environments: [dev]
    resourcePreset: small
tasks:
  migrate:
    from: api
    "on": preDeploy
    command: ["true"]
    fanout: 4
environments:
  dev: {}

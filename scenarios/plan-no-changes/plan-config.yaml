# Planning only. before.yaml and deployah.yaml are identical, so the
# render is the same resource set and the plan must be empty. A second
# plan against an unchanged spec is empty too. Deployment execution is
# independent of this plan result.
freshInstall: false

changes: []

summary:
  add: 0
  change: 0
  destroy: 0

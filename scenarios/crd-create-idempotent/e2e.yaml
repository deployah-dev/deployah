# $schema: ../../internal/testing/e2e.schema.json
env: dev
parallel: false
steps:
  - deploy: {}
    resources:
      - match:
          apiVersion: apiextensions.k8s.io/v1
          kind: CustomResourceDefinition
          metadata:
            name: idempotentwidgets.example.com
          status:
            conditions:
              - type: Established
                status: "True"
      - match:
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: crd-create-idempotent-dev-web
          status:
            readyReplicas: 1
  - deploy: {}
    stderrContains: Deployed
    resources:
      - match:
          apiVersion: apiextensions.k8s.io/v1
          kind: CustomResourceDefinition
          metadata:
            name: idempotentwidgets.example.com
          status:
            conditions:
              - type: Established
                status: "True"

# $schema: ../../internal/spec/schema/v1-alpha.5/manifest.json
apiVersion: v1-alpha.5
project: crd-create-idempotent
components:
  web:
    image: nginx:latest
    port: 80
    environments: [dev]
    resourcePreset: small
environments:
  dev: {}

# $schema: ../../internal/spec/schema/v1-alpha.3/manifest.json
apiVersion: v1-alpha.3
project: plan-command-change
components:
  api:
    image: ghcr.io/acme/api:1.0.0
    port: 8080
    resourcePreset: small
    environments: [production]
    command: ["/bin/api"]
    args: ["--mode=serve", "--workers=2"]
environments:
  production: {}

# $schema: ../../internal/spec/schema/platform/v1-alpha.1/platform.json
apiVersion: platform/v1-alpha.1
environments:
  production:
    domains:
      public:
        baseDomain: apex-example.com
        tls:
          mode: certManager
          issuer: letsencrypt-prod

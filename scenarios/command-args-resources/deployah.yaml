apiVersion: v1-alpha.2
project: command-args-resources
components:
  processor:
    image: ghcr.io/acme/processor:1.0.0
    environments: [production]
    command: ["/bin/sh", "-c"]
    args: ["./run.sh", "--verbose"]
    resources:
      cpu: 250m
      memory: 256Mi
environments:
  production: {}

# Label pull requests from changed paths (.github/labeler.yml).
# https://github.com/actions/labeler
name: Pull Request Labeler

on:
  - pull_request_target

permissions:
  contents: read

jobs:
  labeler:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/labeler@b8dd2d9be0f68b860e7dae5dae7d772984eacd6d # v6
